/**
 * Services Index - Study Service
 */
const subjectService = require('./subject.service');
const sessionService = require('./session.service');
const taskService = require('./task.service');

module.exports = {
  subjectService,
  sessionService,
  taskService,
};
